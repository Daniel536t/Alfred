// This file is @generated by prost-build.
/// A single encrypted input value.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct EncryptedInput {
    /// FHE ciphertext bytes (encrypted under the request's network_encryption_public_key).
    #[prost(bytes = "vec", tag = "1")]
    pub ciphertext_bytes: ::prost::alloc::vec::Vec<u8>,
    /// FHE type discriminator (0-44). Validated server-side.
    #[prost(uint32, tag = "2")]
    pub fhe_type: u32,
}
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct CreateInputRequest {
    /// Target chain where the ciphertexts should be created.
    #[prost(enumeration = "Chain", tag = "1")]
    pub chain: i32,
    /// One or more encrypted input values to create on-chain.
    #[prost(message, repeated, tag = "2")]
    pub inputs: ::prost::alloc::vec::Vec<EncryptedInput>,
    /// ZK proof of valid encryption covering all inputs (skipped in dev mode).
    #[prost(bytes = "vec", tag = "3")]
    pub proof: ::prost::alloc::vec::Vec<u8>,
    /// Who can use these ciphertexts (program/contract address or user address).
    /// Shared across all inputs in this batch.
    #[prost(bytes = "vec", tag = "4")]
    pub authorized: ::prost::alloc::vec::Vec<u8>,
    /// Network encryption public key all inputs were encrypted under.
    /// 32 bytes. Must match an active key registered on-chain.
    #[prost(bytes = "vec", tag = "5")]
    pub network_encryption_public_key: ::prost::alloc::vec::Vec<u8>,
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct CreateInputResponse {
    /// On-chain ciphertext identifiers, one per input (same order as request).
    #[prost(bytes = "vec", repeated, tag = "1")]
    pub ciphertext_identifiers: ::prost::alloc::vec::Vec<::prost::alloc::vec::Vec<u8>>,
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct ReadCiphertextRequest {
    /// BCS-serialized ReadCiphertextMessage (the signed payload).
    #[prost(bytes = "vec", tag = "1")]
    pub message: ::prost::alloc::vec::Vec<u8>,
    /// Ed25519 signature over `message` by the authorized party.
    #[prost(bytes = "vec", tag = "2")]
    pub signature: ::prost::alloc::vec::Vec<u8>,
    /// Public key of the signer (32 bytes).
    /// Must match the ciphertext's `authorized` field, or the ciphertext must be public.
    #[prost(bytes = "vec", tag = "3")]
    pub signer: ::prost::alloc::vec::Vec<u8>,
}
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct ReadCiphertextResponse {
    /// Production: ciphertext re-encrypted under the user's re-encryption key.
    /// The user decrypts locally with their private key.
    /// Mock: raw plaintext bytes (fhe_type determines interpretation).
    #[prost(bytes = "vec", tag = "1")]
    pub value: ::prost::alloc::vec::Vec<u8>,
    /// FHE type of the ciphertext.
    #[prost(uint32, tag = "2")]
    pub fhe_type: u32,
    /// On-chain digest (for client-side verification).
    #[prost(bytes = "vec", tag = "3")]
    pub digest: ::prost::alloc::vec::Vec<u8>,
}
/// Chain identifier.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum Chain {
    Solana = 0,
}
impl Chain {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Solana => "SOLANA",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "SOLANA" => Some(Self::Solana),
            _ => None,
        }
    }
}
/// Generated client implementations.
pub mod encrypt_service_client {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    use tonic::codegen::http::Uri;
    /// Client-facing gRPC API for the Encrypt executor.
    ///
    /// Same API across all chains and environments (local dev, devnet, testnet, mainnet).
    #[derive(Debug, Clone)]
    pub struct EncryptServiceClient<T> {
        inner: tonic::client::Grpc<T>,
    }
    impl EncryptServiceClient<tonic::transport::Channel> {
        /// Attempt to create a new client by connecting to a given endpoint.
        pub async fn connect<D>(dst: D) -> Result<Self, tonic::transport::Error>
        where
            D: TryInto<tonic::transport::Endpoint>,
            D::Error: Into<StdError>,
        {
            let conn = tonic::transport::Endpoint::new(dst)?.connect().await?;
            Ok(Self::new(conn))
        }
    }
    impl<T> EncryptServiceClient<T>
    where
        T: tonic::client::GrpcService<tonic::body::Body>,
        T::Error: Into<StdError>,
        T::ResponseBody: Body<Data = Bytes> + std::marker::Send + 'static,
        <T::ResponseBody as Body>::Error: Into<StdError> + std::marker::Send,
    {
        pub fn new(inner: T) -> Self {
            let inner = tonic::client::Grpc::new(inner);
            Self { inner }
        }
        pub fn with_origin(inner: T, origin: Uri) -> Self {
            let inner = tonic::client::Grpc::with_origin(inner, origin);
            Self { inner }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> EncryptServiceClient<InterceptedService<T, F>>
        where
            F: tonic::service::Interceptor,
            T::ResponseBody: Default,
            T: tonic::codegen::Service<
                http::Request<tonic::body::Body>,
                Response = http::Response<
                    <T as tonic::client::GrpcService<tonic::body::Body>>::ResponseBody,
                >,
            >,
            <T as tonic::codegen::Service<
                http::Request<tonic::body::Body>,
            >>::Error: Into<StdError> + std::marker::Send + std::marker::Sync,
        {
            EncryptServiceClient::new(InterceptedService::new(inner, interceptor))
        }
        /// Compress requests with the given encoding.
        ///
        /// This requires the server to support it otherwise it might respond with an
        /// error.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.send_compressed(encoding);
            self
        }
        /// Enable decompressing responses.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.accept_compressed(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_decoding_message_size(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_encoding_message_size(limit);
            self
        }
        /// Submit one or more encrypted input ciphertexts.
        ///
        /// A single ZK proof covers all inputs in the batch. All inputs share
        /// the same authorized party and network encryption key. The executor
        /// validates the proof (mock in dev mode), creates each ciphertext on-chain,
        /// and returns their identifiers ready for immediate use.
        pub async fn create_input(
            &mut self,
            request: impl tonic::IntoRequest<super::CreateInputRequest>,
        ) -> std::result::Result<
            tonic::Response<super::CreateInputResponse>,
            tonic::Status,
        > {
            self.inner
                .ready()
                .await
                .map_err(|e| {
                    tonic::Status::unknown(
                        format!("Service was not ready: {}", e.into()),
                    )
                })?;
            let codec = tonic_prost::ProstCodec::default();
            let path = http::uri::PathAndQuery::from_static(
                "/encrypt.v1.EncryptService/CreateInput",
            );
            let mut req = request.into_request();
            req.extensions_mut()
                .insert(GrpcMethod::new("encrypt.v1.EncryptService", "CreateInput"));
            self.inner.unary(req, path, codec).await
        }
        /// Read a ciphertext off-chain.
        ///
        /// The request contains a BCS-serialized ReadCiphertextMessage signed by
        /// the authorized party (or anyone if the ciphertext is public).
        ///
        /// In production: returns the ciphertext re-encrypted under the user's
        /// re-encryption key (the user decrypts locally with their private key).
        /// In mock mode: returns the plaintext value directly.
        ///
        /// Responses are cached per (ciphertext_identifier, signer, epoch).
        /// When the epoch advances, the cache is invalidated and old signatures
        /// are rejected.
        pub async fn read_ciphertext(
            &mut self,
            request: impl tonic::IntoRequest<super::ReadCiphertextRequest>,
        ) -> std::result::Result<
            tonic::Response<super::ReadCiphertextResponse>,
            tonic::Status,
        > {
            self.inner
                .ready()
                .await
                .map_err(|e| {
                    tonic::Status::unknown(
                        format!("Service was not ready: {}", e.into()),
                    )
                })?;
            let codec = tonic_prost::ProstCodec::default();
            let path = http::uri::PathAndQuery::from_static(
                "/encrypt.v1.EncryptService/ReadCiphertext",
            );
            let mut req = request.into_request();
            req.extensions_mut()
                .insert(GrpcMethod::new("encrypt.v1.EncryptService", "ReadCiphertext"));
            self.inner.unary(req, path, codec).await
        }
    }
}
/// Generated server implementations.
pub mod encrypt_service_server {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    /// Generated trait containing gRPC methods that should be implemented for use with EncryptServiceServer.
    #[async_trait]
    pub trait EncryptService: std::marker::Send + std::marker::Sync + 'static {
        /// Submit one or more encrypted input ciphertexts.
        ///
        /// A single ZK proof covers all inputs in the batch. All inputs share
        /// the same authorized party and network encryption key. The executor
        /// validates the proof (mock in dev mode), creates each ciphertext on-chain,
        /// and returns their identifiers ready for immediate use.
        async fn create_input(
            &self,
            request: tonic::Request<super::CreateInputRequest>,
        ) -> std::result::Result<
            tonic::Response<super::CreateInputResponse>,
            tonic::Status,
        >;
        /// Read a ciphertext off-chain.
        ///
        /// The request contains a BCS-serialized ReadCiphertextMessage signed by
        /// the authorized party (or anyone if the ciphertext is public).
        ///
        /// In production: returns the ciphertext re-encrypted under the user's
        /// re-encryption key (the user decrypts locally with their private key).
        /// In mock mode: returns the plaintext value directly.
        ///
        /// Responses are cached per (ciphertext_identifier, signer, epoch).
        /// When the epoch advances, the cache is invalidated and old signatures
        /// are rejected.
        async fn read_ciphertext(
            &self,
            request: tonic::Request<super::ReadCiphertextRequest>,
        ) -> std::result::Result<
            tonic::Response<super::ReadCiphertextResponse>,
            tonic::Status,
        >;
    }
    /// Client-facing gRPC API for the Encrypt executor.
    ///
    /// Same API across all chains and environments (local dev, devnet, testnet, mainnet).
    #[derive(Debug)]
    pub struct EncryptServiceServer<T> {
        inner: Arc<T>,
        accept_compression_encodings: EnabledCompressionEncodings,
        send_compression_encodings: EnabledCompressionEncodings,
        max_decoding_message_size: Option<usize>,
        max_encoding_message_size: Option<usize>,
    }
    impl<T> EncryptServiceServer<T> {
        pub fn new(inner: T) -> Self {
            Self::from_arc(Arc::new(inner))
        }
        pub fn from_arc(inner: Arc<T>) -> Self {
            Self {
                inner,
                accept_compression_encodings: Default::default(),
                send_compression_encodings: Default::default(),
                max_decoding_message_size: None,
                max_encoding_message_size: None,
            }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> InterceptedService<Self, F>
        where
            F: tonic::service::Interceptor,
        {
            InterceptedService::new(Self::new(inner), interceptor)
        }
        /// Enable decompressing requests with the given encoding.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.accept_compression_encodings.enable(encoding);
            self
        }
        /// Compress responses with the given encoding, if the client supports it.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.send_compression_encodings.enable(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.max_decoding_message_size = Some(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.max_encoding_message_size = Some(limit);
            self
        }
    }
    impl<T, B> tonic::codegen::Service<http::Request<B>> for EncryptServiceServer<T>
    where
        T: EncryptService,
        B: Body + std::marker::Send + 'static,
        B::Error: Into<StdError> + std::marker::Send + 'static,
    {
        type Response = http::Response<tonic::body::Body>;
        type Error = std::convert::Infallible;
        type Future = BoxFuture<Self::Response, Self::Error>;
        fn poll_ready(
            &mut self,
            _cx: &mut Context<'_>,
        ) -> Poll<std::result::Result<(), Self::Error>> {
            Poll::Ready(Ok(()))
        }
        fn call(&mut self, req: http::Request<B>) -> Self::Future {
            match req.uri().path() {
                "/encrypt.v1.EncryptService/CreateInput" => {
                    #[allow(non_camel_case_types)]
                    struct CreateInputSvc<T: EncryptService>(pub Arc<T>);
                    impl<
                        T: EncryptService,
                    > tonic::server::UnaryService<super::CreateInputRequest>
                    for CreateInputSvc<T> {
                        type Response = super::CreateInputResponse;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<super::CreateInputRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as EncryptService>::create_input(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = CreateInputSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                "/encrypt.v1.EncryptService/ReadCiphertext" => {
                    #[allow(non_camel_case_types)]
                    struct ReadCiphertextSvc<T: EncryptService>(pub Arc<T>);
                    impl<
                        T: EncryptService,
                    > tonic::server::UnaryService<super::ReadCiphertextRequest>
                    for ReadCiphertextSvc<T> {
                        type Response = super::ReadCiphertextResponse;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<super::ReadCiphertextRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as EncryptService>::read_ciphertext(&inner, request)
                                    .await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = ReadCiphertextSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                _ => {
                    Box::pin(async move {
                        let mut response = http::Response::new(
                            tonic::body::Body::default(),
                        );
                        let headers = response.headers_mut();
                        headers
                            .insert(
                                tonic::Status::GRPC_STATUS,
                                (tonic::Code::Unimplemented as i32).into(),
                            );
                        headers
                            .insert(
                                http::header::CONTENT_TYPE,
                                tonic::metadata::GRPC_CONTENT_TYPE,
                            );
                        Ok(response)
                    })
                }
            }
        }
    }
    impl<T> Clone for EncryptServiceServer<T> {
        fn clone(&self) -> Self {
            let inner = self.inner.clone();
            Self {
                inner,
                accept_compression_encodings: self.accept_compression_encodings,
                send_compression_encodings: self.send_compression_encodings,
                max_decoding_message_size: self.max_decoding_message_size,
                max_encoding_message_size: self.max_encoding_message_size,
            }
        }
    }
    /// Generated gRPC service name
    pub const SERVICE_NAME: &str = "encrypt.v1.EncryptService";
    impl<T> tonic::server::NamedService for EncryptServiceServer<T> {
        const NAME: &'static str = SERVICE_NAME;
    }
}
